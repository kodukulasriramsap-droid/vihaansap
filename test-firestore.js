const admin = require('firebase-admin');
const serviceAccount = require('./service-account.json'); // Wait, is there a service account here?
// Actually I don't know if there is a service account. Let me use the firebase-admin already in the project.

