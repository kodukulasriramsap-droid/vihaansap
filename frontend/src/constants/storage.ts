/**
 * localStorage and sessionStorage key constants.
 * Use these instead of hardcoded string literals.
 */
export const STORAGE_KEYS = {
  MOCK_DB: 'srivihaan_mock_db_v2',
  ADMIN_AUTH: 'admin_auth',
} as const;
